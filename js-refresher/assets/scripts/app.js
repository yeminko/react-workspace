import defaultExport, { apiKey, url as link } from "./util.js";
import * as util from "./util.js";

console.log(defaultExport);
console.log(apiKey);
console.log(link);

// Same as above, but using the util object
console.log(util.default);
console.log(util.apiKey);
console.log(util.url);
